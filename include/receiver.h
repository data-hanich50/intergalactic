#include <stdint.h>
#include <stddef.h>
#include "common.h"

/**
 * @brief Transmits a given message
 *
 * @param[in] data list of bytes that contains encoded message
 * @param[in, out] len length of data
 * will be returned the decoded message length
 * @param[out] decoded will contain the decoded message
 *
 * @return return code of function
 */
return_code_t receiver__receive(uint8_t *data, size_t *len, uint8_t **decoded);
